diff -ruN vf_ci_orig/components/videoflow/VideoFlowApp.tsx vf_ci_patch/components/videoflow/VideoFlowApp.tsx
--- vf_ci_orig/components/videoflow/VideoFlowApp.tsx	2026-08-30 16:42:38.000000000 +0000
+++ vf_ci_patch/components/videoflow/VideoFlowApp.tsx	2026-08-30 19:20:10.739007833 +0000
@@ -147,6 +147,8 @@
   WatermarkMask,
 } from "@/lib/videoflow/types";
 
+type WatermarkTrackingPoint = NonNullable<NonNullable<WatermarkMask["ai"]>["tracking"]>[number];
+
 type View =
   | "dashboard"
   | "editor"
@@ -2462,9 +2464,9 @@
         manual: true,
       };
       const tracking = next.ai.tracking
-        .filter((point) => Math.abs(point.time - playhead) >= 0.001)
+        .filter((point: WatermarkTrackingPoint) => Math.abs(point.time - playhead) >= 0.001)
         .concat(correction)
-        .sort((a, b) => a.time - b.time);
+        .sort((a: WatermarkTrackingPoint, b: WatermarkTrackingPoint) => a.time - b.time);
       next = { ...next, ai: { ...next.ai, tracking } };
     }
     change(label, {
@@ -3300,7 +3302,7 @@
             </p>
           </section>
           <AISettingsPanel />
-          <StorageStatus project={project} onReload={() => openStored(project.id, project)} />
+          <StorageStatus project={project} onReload={() => onOpen(project.id)} />
           <section>
             <h2>About</h2>
             <p>
diff -ruN vf_ci_orig/lib/videoflow/export.ts vf_ci_patch/lib/videoflow/export.ts
--- vf_ci_orig/lib/videoflow/export.ts	2026-08-30 16:40:03.000000000 +0000
+++ vf_ci_patch/lib/videoflow/export.ts	2026-08-30 19:20:10.739257592 +0000
@@ -133,6 +133,15 @@
   });
 }
 
+async function writeBytesToDisk(
+  writable: FileSystemWritableFileStream,
+  bytes: Uint8Array,
+): Promise<void> {
+  const copy = new Uint8Array(bytes.byteLength);
+  copy.set(bytes);
+  await writable.write(copy.buffer);
+}
+
 async function clearFailedDiskOutput(handle: FileSystemFileHandle): Promise<void> {
   try {
     const writable = await handle.createWritable();
@@ -215,7 +224,7 @@
       const segmentBytes = new Uint8Array(await segmentBlob.arrayBuffer());
       if (!state) {
         state = createFragmentedMp4State(segmentBytes);
-        await writable.write(state.initSegment);
+        await writeBytesToDisk(writable, state.initSegment);
         bytesWritten += state.initSegment.byteLength;
       } else {
         assertCompatibleFragmentInit(segmentBytes, state);
@@ -223,7 +232,7 @@
       const chunks = mediaChunksForSegment(segmentBytes, state, start);
       for (const chunk of chunks) {
         if (signal.aborted) throw new DOMException("Export cancelled.", "AbortError");
-        await writable.write(chunk);
+        await writeBytesToDisk(writable, chunk);
         bytesWritten += chunk.byteLength;
       }
       onProgress(
diff -ruN vf_ci_orig/lib/videoflow/ffmpeg-commands.d.mts vf_ci_patch/lib/videoflow/ffmpeg-commands.d.mts
--- vf_ci_orig/lib/videoflow/ffmpeg-commands.d.mts	2026-08-30 14:16:37.000000000 +0000
+++ vf_ci_patch/lib/videoflow/ffmpeg-commands.d.mts	2026-08-30 19:20:10.737823226 +0000
@@ -14,7 +14,7 @@
   gifFps?: number;
   gifWidth?: number;
 }): string[];
-export function buildProxyArgs(input?: string, output?: string): string[];
+export function buildProxyArgs(input?: string, output?: string, options?: { width?: number; height?: number; fps?: number; crf?: number }): string[];
 export function buildStabilizeArgs(options?: {
   input?: string;
   output?: string;
diff -ruN vf_ci_orig/lib/videoflow/ffmpeg.ts vf_ci_patch/lib/videoflow/ffmpeg.ts
--- vf_ci_orig/lib/videoflow/ffmpeg.ts	2026-08-30 16:16:08.000000000 +0000
+++ vf_ci_patch/lib/videoflow/ffmpeg.ts	2026-08-30 19:20:10.739358054 +0000
@@ -117,7 +117,9 @@
   // Avoid an explicit second Uint8Array copy. The FFmpeg MEMFS result still
   // exists in memory until cleanup, so large-output preflight must keep this
   // pathway below the configured browser-risk threshold.
-  return new Blob([data], { type: mime });
+  const copy = new Uint8Array(data.byteLength);
+  copy.set(data);
+  return new Blob([copy.buffer], { type: mime });
 }
 
 async function deleteQuietly(instance: FFmpeg, names: string[]): Promise<void> {
@@ -479,9 +481,14 @@
         const asset = generated ? null : assetById.get(sourceId);
         if (!generated && !asset)
           throw new Error(`A timeline source is missing from storage: ${sourceId}`);
-        const sourceBlob = generated ? await makeOverlayBlob(plan, sourceId) : asset!.blob;
-        if (!generated && !sourceBlob) {
-          throw new Error(`Original media is offline: ${asset!.name}. Relink the original before final export.`);
+        const sourceBlob: Blob | undefined = generated
+          ? await makeOverlayBlob(plan, sourceId)
+          : asset!.blob;
+        if (!sourceBlob) {
+          if (!generated) {
+            throw new Error(`Original media is offline: ${asset!.name}. Relink the original before final export.`);
+          }
+          throw new Error(`Generated overlay source is unavailable: ${sourceId}`);
         }
         const kind = generated ? (sourceId.startsWith("text:") ? "text" : "subtitle") : asset!.kind;
         const extension = generated ? "png" : safeExtension(asset!.name);
diff -ruN vf_ci_orig/lib/videoflow/import-policy.mjs vf_ci_patch/lib/videoflow/import-policy.mjs
--- vf_ci_orig/lib/videoflow/import-policy.mjs	2026-08-30 16:38:30.000000000 +0000
+++ vf_ci_patch/lib/videoflow/import-policy.mjs	2026-08-30 19:20:10.737658656 +0000
@@ -68,7 +68,7 @@
 
 /**
  * Structural render-risk estimate used before local export.
- * @param {{width:number,height:number,fps:number,duration:number,videoBitrate:number,effectComplexity?:number,clipCount?:number}} input
+ * @param {{width:number,height:number,fps:number,duration:number,videoBitrate:number,effectComplexity?:number,clipCount?:number,aiEffects?:boolean,directDiskAvailable?:boolean,deviceMemoryGb?:number}} input
  */
 export function assessRenderRisk(input) {
   const pixels = Math.max(1, input.width * input.height);
diff -ruN vf_ci_orig/lib/videoflow/media.ts vf_ci_patch/lib/videoflow/media.ts
--- vf_ci_orig/lib/videoflow/media.ts	2026-08-30 16:14:29.000000000 +0000
+++ vf_ci_patch/lib/videoflow/media.ts	2026-08-30 19:20:10.739422271 +0000
@@ -211,7 +211,7 @@
     const current = await handle.queryPermission?.({ mode: "read" });
     if (!current || current === "granted") return true;
     if (current === "denied" && !request) return false;
-    if (!request) return current === "granted";
+    if (!request) return false;
     return (await handle.requestPermission?.({ mode: "read" })) === "granted";
   } catch {
     return false;
diff -ruN vf_ci_orig/tests/e2e/videoflow.spec.ts vf_ci_patch/tests/e2e/videoflow.spec.ts
--- vf_ci_orig/tests/e2e/videoflow.spec.ts	2026-08-30 16:47:18.000000000 +0000
+++ vf_ci_patch/tests/e2e/videoflow.spec.ts	2026-08-30 19:20:10.739503304 +0000
@@ -234,7 +234,7 @@
   await expect(page.getByText(/3840×2160/)).toBeVisible();
 });
 
-test("bundled AI executes a genuine local neural preview with tracking and offline reuse", async ({ page, context }) => {
+test("bundled AI executes a genuine local neural preview with tracking and offline reuse", async ({ page, context, browserName }) => {
   test.setTimeout(300_000);
   const aiFixture = join(root, "tests", "fixtures", "ai", "ai-static-watermark-720p.mp4");
   await importMedia(page, aiFixture);
