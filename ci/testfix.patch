diff -ruN vf_ci_lintbase/.github/workflows/release-verification.yml vf_ci_patch/.github/workflows/release-verification.yml
--- vf_ci_lintbase/.github/workflows/release-verification.yml	2026-08-30 16:52:00.000000000 +0000
+++ vf_ci_patch/.github/workflows/release-verification.yml	2026-08-30 19:29:16.943793815 +0000
@@ -33,10 +33,6 @@
         run: npx tsc --noEmit
       - name: Lint
         run: npm run lint
-      - name: Portable unit and architecture tests
-        run: |
-          mkdir -p ci-results
-          npm run test:unit | tee ci-results/unit.log
       - name: Render-plan verification
         run: npm run verify:render
       - name: Fresh production build
@@ -47,6 +43,10 @@
         env:
           VIDEOFLOW_REQUIRE_AI_PACK: "1"
         run: npm run verify:dist
+      - name: Unit, architecture, static-dist and fragmented-media tests
+        run: |
+          mkdir -p ci-results
+          npm run test:unit | tee ci-results/unit.log
       - name: Install Playwright browsers
         run: npx playwright install --with-deps chromium firefox webkit
       - name: Chromium / Firefox / WebKit E2E
diff -ruN vf_ci_lintbase/scripts/generate-test-fixtures.mjs vf_ci_patch/scripts/generate-test-fixtures.mjs
--- vf_ci_lintbase/scripts/generate-test-fixtures.mjs	2026-08-30 16:29:00.000000000 +0000
+++ vf_ci_patch/scripts/generate-test-fixtures.mjs	2026-08-30 19:29:16.943514081 +0000
@@ -83,6 +83,28 @@
   if (result.status !== 0) throw new Error(result.stderr);
 }
 
+
+const fragmentFiles = [0, 1, 2].map((index) => join(directory, `fragment-seg${index}.mp4`));
+for (const [index, fragment] of fragmentFiles.entries()) {
+  if (existsSync(fragment)) continue;
+  const result = spawnSync(
+    "ffmpeg",
+    [
+      "-hide_banner", "-loglevel", "error", "-y",
+      "-f", "lavfi", "-i", `testsrc2=s=320x180:r=30:d=2`,
+      "-f", "lavfi", "-i", `sine=frequency=${440 + index * 110}:sample_rate=48000:duration=2`,
+      "-t", "2",
+      "-c:v", "libx264", "-preset", "ultrafast", "-crf", "24", "-pix_fmt", "yuv420p",
+      "-g", "60", "-keyint_min", "60", "-sc_threshold", "0",
+      "-c:a", "aac", "-b:a", "128k", "-ar", "48000", "-ac", "2",
+      "-movflags", "+frag_keyframe+empty_moov+default_base_moof+omit_tfhd_offset",
+      fragment,
+    ],
+    { encoding: "utf8" },
+  );
+  if (result.status !== 0) throw new Error(result.stderr);
+}
+
 const aiDir = join(process.cwd(), "tests", "fixtures", "ai");
 mkdirSync(aiDir, { recursive: true });
 const aiStatic = join(aiDir, "ai-static-watermark-720p.mp4");
